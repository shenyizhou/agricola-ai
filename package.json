{
  "name": "agricola-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Agricola board game engine, MCTS AI, and browser UI",
  "scripts": {
    "build": "node scripts/build-browser.js && node scripts/build-standalone.js",
    "build:browser": "node scripts/build-browser.js",
    "build:standalone": "node scripts/build-standalone.js"
  }
}
